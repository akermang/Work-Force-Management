import React from 'react';
import styles from './header.component.scss';

const HeaderComponent = () => (
  <header className={styles.header}>React redux starter</header>
);

export default HeaderComponent;